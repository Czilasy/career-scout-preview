# 安全政策

## 报告安全漏洞

如果你发现 Career Scout 存在安全漏洞，**请不要直接公开 issue**，以免在修复前暴露风险。

### 报告方式

通过 GitHub 私密渠道报告：

1. 打开仓库页面 → **Security** 选项卡 → **Report a vulnerability**（如已开启 Private vulnerability reporting）；
2. 或发送邮件到 `czyooutzilas@gmail.com`，标题注明 `[SECURITY] Career Scout`。

### 报告内容建议

- 漏洞类型与影响范围
- 复现步骤（越具体越好）
- 受影响的版本或提交
- 如有可能，附上修复建议

### 响应承诺

- 收到报告后 7 天内确认接收
- 确认修复方案后尽快发布修复版本
- 修复公开后会在更新日志中致谢报告者（除非报告者要求匿名）

## 安全边界说明

本项目在设计上遵循以下安全原则，报告时可参考：

- **本地优先**：Web 工作台仅监听本机（`127.0.0.1`），职位数据、简历均在本地处理。
- **凭据隔离**：AI Key 通过系统凭据库保存（Windows Credential Manager / macOS Keychain / Linux Secret Service），不写入 SQLite、日志或导出文件。
- **会话令牌**：简历读取、AI 设置读写等接口使用本地会话令牌保护。
- **链接白名单**：前端只打开岗位所属平台域名（`zhipin.com` / `zhaopin.com`）且通过平台规则校验的 HTTPS 链接，不接受任意外部地址。
- **如实失败**：风控、验证码、登录过期、限流等情况会区分原因如实展示，不伪装成功。
- **日志脱敏**：本地日志只记录脱敏后的诊断上下文，不包含 API Key、Cookie、简历/JD 正文；AI 原始响应不落盘。

## 不属于漏洞的情况

- 抓取目标网站时被平台风控或验证码拦截（这是目标网站的正常防护，不是本项目漏洞）。
- 用户自行将 AI Key、Cookie 等敏感信息提交到仓库（仓库卫生规则已禁止，见 CONTRIBUTING.md）。
- 用户本机其他软件导致的环境泄露。
