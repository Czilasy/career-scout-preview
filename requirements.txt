websocket-client>=1.6.0,<2.0.0
requests>=2.28.0,<3.0.0
flask>=3.0.0,<4.0.0
keyring>=24.0.0,<26.0.0
pypdf>=4.0.0,<6.0.0
python-docx>=1.1.0,<2.0.0

# 002 简历驱动的两层筛选：复用上述依赖，无新增第三方依赖。
# 筛选项枚举复用 scripts/boss_cdp_raw.py 的映射；硬规则核验为纯逻辑；
# AI 读简历与语义相似度占位复用 webui/ai.py；持久化复用 webui/store.py。
