websocket-client>=1.6.0,<2.0.0
requests>=2.28.0,<3.0.0
flask>=3.0.0,<4.0.0
keyring>=24.0.0,<26.0.0
pypdf>=4.0.0,<6.0.0
python-docx>=1.1.0,<2.0.0

# PDF/DOCX 简历在送入 AI API 前需先转为纯文本（API 只接受文本）。
# 这里的转换只是传输准备，不做任何内容理解。
