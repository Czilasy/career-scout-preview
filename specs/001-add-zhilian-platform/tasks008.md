# Tasks 008：最终集成、真实验收与交付收口

## 新会话启动提示词

```text
请在当前仓库根目录执行 specs\001-add-zhilian-platform\tasks008.md。先读取仓库根目录 AGENTS.md、智联功能目录全部冻结工件，以及 tasks001.md 至 tasks007.md；正式实施前输出【已查阅】。

本会话只负责最终集成和验收，不重新设计功能。不要相信任务文件勾选或旧会话自述，必须逐项核验 tasks001 至 tasks007 的实际代码、独立提交、聚焦测试和未关闭阻断。任一模块不完整时停止最终全量验收，明确指出应回到哪个 tasks 文件的哪个节点，不要在 tasks008 中无边界重写。只有外部事实门禁、migration、BOSS 回归、智联真实一页主链、失败恢复、外围操作、调优、Legacy、双视口和最终全量都有客观证据时才能宣布完成。按文件规定执行一次独立严格审查和聚焦复查。最终只提交集成修复，不自动 push，除非用户在该新会话明确要求。
```

## 给独立执行 AI 的指令

本任务是唯一最终集成任务，不是并行开发任务。新会话必须读取根规则、全部冻结工件和 `tasks001.md` 至 `tasks007.md`。不得因任务文件已打勾就认定前置完成，必须基于最终工作区、git 历史和测试现场核验。

## 总前置门禁

逐个核验 tasks001-007 均有独立提交、允许范围内的代码、对应聚焦测试和无未关闭阻断。外部事实门禁必须明确：公司性质、验收城市、列表、详情、空状态和风控 fixture 当前有效。任一模块不完整时停止最终全量验收，回到对应 tasks 文件的缺失节点；不得在本任务无边界重写模块。

## 允许与禁止范围

允许修改：只处理模块组合产生的接口不一致、低风险集成缺陷、平台注册启用/禁用接线和最终测试。跨模块缺陷应归属原模块并做聚焦修复。

禁止修改：冻结 Spec、验收标准或错误矩阵；禁止以删除测试、放宽校验、默认 BOSS、伪 fixture 或跳过真实主链来收口。禁止恢复旧 v26 备份覆盖已有智联数据。

## 节点门禁 A：静态合同组合

检查平台注册表、store、source、编排、前端和调优对 platform、platform_job_id、job_id、状态和错误码使用一致。发现错配时先定位所有者，运行对应聚焦测试修复，再继续；不得直接进入全量测试。

- [x] T701 核对三个合同与最终路由、类型、store 字段、source outcome、前端类型和调优 manifest 一致
- [x] T702 核对 BOSS/智联 URL 规则、城市码、profile/端口、schema 字段和 enabled_for_new_tasks 均来自注册表
- [x] T703 核对所有 source 入口从目标 run/manifest 继承平台，所有收藏反馈只使用内部 UUID
- [x] T704 运行各模块聚焦测试，先收敛全部已知失败，再进入首次最终全量

## 节点门禁 B：Migration 与 BOSS 回归

必须使用独立 v26 数据副本。先记录行数、旧 UUID、收藏/反馈关系，再启动新代码。备份、manifest 或校验任一步失败都必须阻断启动。

- [x] T705 执行 migration 27 正常、幂等、备份失败和中途失败回滚验收，并核对数据守恒
- [x] T706 验证存量调优 JSON/digest 未改写、旧 source outcome 未猜造、全局 URL 唯一和双索引冲突
- [x] T707 执行现有 BOSS 自动化全量和一页主链冒烟，确认搜索、JD、AI、结果、恢复、收藏反馈不回归

## 节点门禁 C：智联真实主链

只有平台启用所需外部事实全部确认、人工登录态有效、测试数据库/profile 边界明确时才运行。若遇到登录墙、验证、限流或封禁，只能验证 paused 与恢复，不得绕过。

- [x] T708 运行一个关键词、一个已验证城市、一页智联列表，核对页面可见岗位字段、平台 ID 和规范链接
- [x] T709 至少取得一个真实 JD，完成 AI 粗筛、精筛和结果分组，并核对完整筛选快照
- [x] T710 刷新并重启后核对 source attempts、经验/学历/extra、平台身份、task digest 和结果仍可审计
- [x] T711 收藏并反馈一个智联岗位，确认先生成内部 UUID且刷新后关联不变

## 节点门禁 D：失败、恢复与外围操作

每个场景都按 frozen error matrix 验证；不得用单次 smoke 替代。目标 run 创建后把草稿切到另一平台，再执行外围操作。

- [x] T712 验证登录、验证、限流、封禁、CDP、网络、超时、结构失效、单详情异常和批详情熔断分支
- [x] T713 验证暂停解除后原子 continue、完成项不重复、重启恢复、digest/schema/runtime 错配阻断
- [x] T714 验证进度、取消、提前结束、单 JD、单项/批量补抓和 reset 始终使用目标 run，且不关闭另一平台或未知 profile
- [x] T715 验证 latest result 三种查询、平台禁用后新执行阻断而历史结果/岗位/收藏/反馈仍可读
- [x] T716 验证全部 legacy 路由显式智联零副作用拒绝和五类调优 round 平台守恒

## 节点门禁 E：前端与最终全量

只有后端真实主链和外围操作通过后才做最终 UI 联调。使用真实渲染验证 1440×900 与 390×844；随后基于最终代码运行一次干净全量。中途出现新失败时回到“定位→聚焦修复→聚焦验证”，已知问题收敛后才重新运行最终全量。

- [x] T717 验证平台切换、专属草稿、100 次异步交错、四类非终态恢复和最近结果不重标
- [x] T718 验证两个视口的空/加载/成功/失败/暂停/partial 状态，无重叠、溢出、逐字竖排或不可达操作
- [x] T719 运行最终 Python 全量、前端全量、构建和仓库卫生检查
- [x] T720 对完整最终差异执行一次独立严格审查，只修阻断项并针对原驳回项聚焦复查

## 最终命令

```powershell
uv run python -m unittest discover -s tests
npm --prefix webui test
npm --prefix webui run build
uv run python -m unittest tests.test_repo_hygiene
```

全部通过后检查 `git status`、`git diff` 和待提交内容，确认无敏感信息、本地绝对路径、运行数据、缓存或构建产物。使用仓库规定邮箱提交最终集成修复；不自动 push。后端改动需要重启时，由执行 AI 重启受影响服务并验证可访问。

## 完成定义

只有逻辑、状态、数据、前端、真实主链、BOSS 回归、调优、legacy、两个视口、最终全量和独立审查均有客观证据时，才可宣布智联模块完成。任一真实环境条件缺失时必须明确列为未验证边界，不能以 fixture 或单点冒烟替代完整验收。
