# 验收清单

## 2026-07-13 第二、三档正式集成补充记录（取代下方占位结论）

下方原始记录保留作为第一档历史基线；其中“AI 语义相似度占位恒过”、
“第二层框架待设计”和旧测试总数已经被本节取代，不再代表当前代码状态。

### 自动化测试

- 命令：`python -m unittest discover -s tests -q`
- 结果：**718 tests OK，0 失败，0 错误**，约 59.0 秒。
- 已覆盖：固定四维语义结构、程序阈值裁决、置信度/无效输出/超时进入待核验、
  薪资交集、学历覆盖与专本互通、逐岗位即时保存、部分抓取保留、单条/批量重试、
  人工分流、垃圾桶原区域恢复、快照清理后恢复运行、30 天清理记录、五个正式视图。
- 最终审查整改覆盖：服务端固定 30 天策略、拒绝/垃圾桶来源原子写入、pending
  暂存与恢复、恢复失败保持垃圾桶可恢复，以及受控运行 JSON 随清理删除。
- 已知测试警告：`ScreeningDOMContractTests` 读取 `index.html` 时产生一个既有
  `ResourceWarning: unclosed file`；测试退出码仍为 0。该警告不等同于功能失败，
  但也不写作“输出无警告”。

### 模拟端到端与程序化浏览器契约

- `python -m unittest tests.test_screening_prototype -v`：**10 tests OK**。
- `python tests/browser_acceptance.py`：**16 项程序化检查全部通过**。
- 以上均为本地固定模拟数据/API/HTML 契约验证，**不构成真实 BOSS 来源成功**。

### 真实来源冒烟

- 状态：**外部环境阻塞，未在本次正式集成中伪造或替代**。
- 阻塞仍为下方记录的专用 Chrome/CDP 登录环境不可用；本次没有绕过登录、验证码、
  频率限制或其他来源限制。

### 需要用户进行的交互验收

- 1366×768 下默认结果区是否能舒适辨识至少 5 条岗位。
- 五视图切换、列表原位更新、批量重试、人工分流、垃圾桶恢复是否顺畅。
- 720px 窄屏下是否无页面级横向滚动，主要操作是否均可触达。
- 实际筛选结果与 AI 语义阈值是否符合用户的求职判断。

本文件记录简历驱动的两层筛选（T001-T065）的自动化测试、浏览器验收、真实来源冒烟测试与安全审查结果。自动化测试与程序化验收已完成；真实来源冒烟测试因无运行中的专用 Chrome 阻塞，需用户手动执行；真实浏览器交互待用户确认。

## 自动化测试

命令：`python -m unittest discover -s tests -v`

结果：**通过（633 tests OK，0 失败，0 错误）**

执行时间：约 36.9 秒

### 按模块明细

| 测试模块 | 测试数 | 覆盖范围 |
|----------|--------|----------|
| tests/test_ai.py | 79 | 读简历给筛选项建议合同、AI 语义相似度占位、AI 可用性检测、连接测试与安全错误返回 |
| tests/test_chrome_setup.py | 32 | Chrome CDP 设置、隔离 profile、登录态、进程解析 |
| tests/test_job_summary.py | 14 | 抓取后摘要脚本与提示词生成 |
| tests/test_readme.py | 38 | README.md 与 README.en.md 文档契约（002 六类主题中英文覆盖） |
| tests/test_resume.py | 39 | 简历存储、提取、路径校验、原子删除 |
| tests/test_screening.py | 143 | 筛选项枚举、字段合并、执行运行状态机、条件冻结、第一层编排、硬规则核验、分流、区域生命周期、排序、展示排除、降级路径 |
| tests/test_screening_fixtures.py | 7 | 筛选功能测试夹具与临时状态目录辅助函数 |
| tests/test_webui_app.py | 112 | 令牌保护、上传简历、读简历填筛、确认执行、两区查询、反馈、感兴趣/垃圾桶列表、降级、DOM 契约 |
| tests/test_webui_browser.py | 25 | 浏览器契约：两区切换、卡片跳转、按钮阻止跳转、垃圾桶查看、降级态 |
| tests/test_webui_core.py | 6 | 参数校验与可解释匹配 |
| tests/test_webui_runner.py | 16 | 后台任务编排与产物校验 |
| tests/test_webui_store.py | 68 | screening_runs/screening_results 迁移、感兴趣/垃圾桶持久化、区域清空、跨简历保留、状态机 |
| tests/test_workbench.py | 26 | 关键词选择、去重、预算、卡片投影、链接规范化 |
| tests/test_workbench_api.py | 28 | 001 工作台接口契约（画像、反馈、历史、AI 设置） |
| **合计** | **633** | 全量覆盖 001 工作台与 002 筛选功能 |

## 场景验收

> 程序化验收通过 `tests/browser_acceptance.py` 完成（T063），验证 API 与 HTML 契约。
> 真实浏览器交互（点击卡片视觉、两区切换动画、模态层弹出等）仍需用户在浏览器中手动确认。

### 场景 1：上传简历并自动填筛

| 验收步骤 | 结果 |
|----------|------|
| 上传可解析简历且 AI 可用时，AI 读取简历并给出筛选栏建议值 | 程序化通过（接口受令牌保护，建议接口可用） |
| 用户可修改任意字段，确认值优先，AI 不得覆盖 | 程序化通过（合并规则测试覆盖） |
| 用户未修改字段采用 AI 建议值 | 程序化通过（合并规则测试覆盖） |
| AI 无法提取且用户未填的字段留空，不参与第一层搜索与第二层核验 | 程序化通过（留空字段测试覆盖） |

### 场景 2：第一层 API 搜索

| 验收步骤 | 结果 |
|----------|------|
| 用户确认条件点击执行后，用确认条件调 BOSS 搜索 API 抓回职位 | 程序化通过（执行接口校验关键词与冻结条件） |
| 第一层抓回职位全部进入第二层核验 | 程序化通过（编排测试覆盖） |

### 场景 3：第二层两层核验

| 验收步骤 | 结果 |
|----------|------|
| 硬规则核验字段与本次确认字段一致，用户选了什么核什么，没选不核，城市在内无强制必填 | 程序化通过（硬规则测试覆盖） |
| AI 语义相似度占位恒过（框架本次不实现，留接口契约） | 程序化通过（占位测试覆盖） |
| 两条都过进符合区 | 程序化通过（分流测试覆盖） |
| 任一不过进不符合区 | 程序化通过（分流测试覆盖） |
| 不符合区混在一起展示，不区分被硬规则还是 AI 排除，不标注原因 | 程序化通过（不标原因测试覆盖） |

### 场景 4：感兴趣与不感兴趣

| 验收步骤 | 结果 |
|----------|------|
| 符合区或不符合区任意卡片点感兴趣，进持久感兴趣区 | 程序化通过（持久化测试覆盖） |
| 符合区或不符合区任意卡片点不感兴趣，进持久垃圾桶区 | 程序化通过（持久化测试覆盖） |
| 后续搜索展示时，曾点不感兴趣的具体岗位被排除 | 程序化通过（展示排除测试覆盖） |
| 排除仅按具体岗位，不扩展到同公司或相似岗位 | 程序化通过（不扩展测试覆盖） |
| 垃圾桶区域可查看曾标记不感兴趣的岗位列表 | 程序化通过（垃圾桶列表接口可用且受令牌保护） |
| 感兴趣区卡片点击在浏览器打开 BOSS 原始页面，仅 HTTPS 且预期 BOSS 域名 | 程序化通过（链接校验拒绝 javascript:、非 BOSS 域名；接受 HTTPS zhipin.com） |

### 场景 5：区域生命周期

| 验收步骤 | 结果 |
|----------|------|
| 符合区与不符合区为本次执行临时区域 | 程序化通过（临时区测试覆盖） |
| 开始下一次执行时清空上一次符合区与不符合区 | 程序化通过（清空测试覆盖） |
| 感兴趣区不受区域清空影响，持久保留 | 程序化通过（持久化测试覆盖） |
| 垃圾桶区域与不感兴趣记录不受区域清空影响，持久保留 | 程序化通过（持久化测试覆盖） |

### 场景 6：符合区排序

| 验收步骤 | 结果 |
|----------|------|
| 符合区岗位先按抓回顺序排列，不使用相似度排序 | 程序化通过（抓回顺序测试覆盖） |
| 第二层 AI 若输出匹配分，未来可基于该分排序无需额外 AI 调用（待框架设计） | 待框架设计（本次不实现） |

### 场景 7：AI 不可用降级

| 验收步骤 | 结果 |
|----------|------|
| AI 不可用时提示用户 AI 不可用 | 程序化通过（首页含 ai-unavailable 降级态元素） |
| 退化为人工填写筛选字段 + 脚本抓取，不执行 AI 读简历与语义相似度 | 程序化通过（降级路径测试覆盖） |
| 上传简历步骤可跳过 | 程序化通过（首页含跳过简历选项） |
| 第二层仅执行硬规则核验，不执行 AI 语义相似度 | 程序化通过（仅硬规则测试覆盖） |

### 程序化验收脚本结果

执行命令：`python tests/browser_acceptance.py`

结果：**全部通过（16 项检查 OK）**

- 验收 A：首页含筛选工作区、筛选选项七类、读简历填筛接口受令牌保护、执行接口校验关键词
- 验收 B：感兴趣/垃圾桶列表接口受令牌保护、反馈接口拒绝 javascript: 与非 BOSS 域名、接受 HTTPS zhipin.com 链接
- 验收 C：符合/不符合区查询接口受令牌保护、未知 run 返回 404
- 验收 D：首页含 AI 不可用降级态元素、跳过简历选项、人工填筛提示
- 验收 E：AI 设置不回显 Key、首页含 HTTPS 与 zhipin.com 校验、无自动投递入口、反馈按钮显式 type=button

**待用户手动确认的真实浏览器交互**：
- 点击卡片阅读区在新标签页打开 BOSS 链接的视觉效果
- 两区切换按钮的视觉高亮与区域切换动画
- 感兴趣区/垃圾桶区模态层的弹出与关闭
- 不感兴趣后卡片的平滑退场与撤销条

## 功能需求核对

### 简历与自动填筛

| FR | 描述 | 结果 |
|----|------|------|
| FR-001 | 允许用户上传一份简历 | 通过（test_webui_app.py 上传接口测试覆盖） |
| FR-002 | 上传后调用 AI 读取简历内容 | 通过（test_ai.py 读简历合同测试覆盖） |
| FR-003 | 同时获取 BOSS 筛选选项枚举 | 通过（test_screening.py 筛选项枚举测试覆盖） |
| FR-004 | AI 判断哪些选项可从简历填入并返回建议值 | 通过（test_ai.py 建议值合同测试覆盖） |
| FR-005 | 用户可修改任意字段，确认值优先 | 通过（test_screening.py 合并规则测试覆盖） |
| FR-006 | 未修改字段采用 AI 建议值 | 通过（test_screening.py 合并规则测试覆盖） |
| FR-007 | AI 无法提取且用户未填的字段留空 | 通过（test_screening.py 留空字段测试覆盖） |

### 第一层 API 搜索

| FR | 描述 | 结果 |
|----|------|------|
| FR-008 | 点击执行后用确认条件调 BOSS 搜索 API | 通过（test_screening.py 第一层编排测试覆盖） |
| FR-009 | 第一层抓回职位全部进入第二层核验 | 通过（test_screening.py 编排测试覆盖） |

### 第二层两层核验

| FR | 描述 | 结果 |
|----|------|------|
| FR-010 | 硬规则核验字段与本次确认字段一致，无强制必填 | 通过（test_screening.py 硬规则测试覆盖） |
| FR-011 | AI 语义相似度判断（占位，框架待设计） | 通过（test_ai.py 占位测试覆盖；恒返回过） |
| FR-012 | AI 约束框架待专门设计，约束 AI 不自由发挥 | 待框架设计（本次不实现） |
| FR-013 | 两条都过进符合区 | 通过（test_screening.py 分流测试覆盖） |
| FR-014 | 任一不过进不符合区 | 通过（test_screening.py 分流测试覆盖） |
| FR-015 | 不符合区不区分原因、不标注 | 通过（test_screening.py 不标原因测试覆盖） |

### 展示与分区

| FR | 描述 | 结果 |
|----|------|------|
| FR-016 | 前端提供符合/不符合两个按钮切换 | 通过（test_webui_app.py DOM 契约测试覆盖） |
| FR-017 | 每张卡片带感兴趣/不感兴趣按钮 | 通过（test_webui_app.py DOM 契约测试覆盖） |

### 感兴趣与不感兴趣

| FR | 描述 | 结果 |
|----|------|------|
| FR-018 | 点感兴趣后进持久感兴趣区 | 通过（test_webui_store.py 持久化测试覆盖） |
| FR-019 | 符合区和不符合区岗位都能被标记感兴趣 | 通过（test_webui_app.py 集成测试覆盖） |
| FR-020 | 感兴趣区卡片可点击跳转 BOSS，仅 HTTPS 预期域名 | 通过（test_screening.py 链接校验测试覆盖；normalize_job_link 拒绝非安全链接） |
| FR-021 | 点不感兴趣后进持久垃圾桶区 | 通过（test_webui_store.py 持久化测试覆盖） |
| FR-022 | 后续搜索展示时排除曾点不感兴趣的具体岗位 | 通过（test_screening.py 展示排除测试覆盖） |
| FR-023 | 排除仅按具体岗位，不扩展 | 通过（test_screening.py 不扩展测试覆盖） |
| FR-024 | 垃圾桶区域可查看 | 通过（test_webui_app.py 垃圾桶列表接口测试覆盖） |

### 区域生命周期

| FR | 描述 | 结果 |
|----|------|------|
| FR-025 | 符合区与不符合区为本次执行临时区域 | 通过（test_webui_store.py 临时区测试覆盖；run_id 隔离实现） |
| FR-026 | 开始下一次执行时清空上一次符合区与不符合区 | 通过（test_webui_store.py 区域隔离测试覆盖；通过 run_id 隔离实现） |
| FR-027 | 感兴趣区持久保留 | 通过（test_webui_store.py 持久化测试覆盖） |
| FR-028 | 垃圾桶区域与不感兴趣记录持久保留 | 通过（test_webui_store.py 持久化测试覆盖） |

### 排序

| FR | 描述 | 结果 |
|----|------|------|
| FR-029 | 符合区按抓回顺序排列，不用相似度排序 | 通过（test_screening.py 抓回顺序测试覆盖） |
| FR-030 | AI 若输出匹配分可未来用于排序（待框架设计） | 待框架设计（本次不实现） |

### AI 不可用降级

| FR | 描述 | 结果 |
|----|------|------|
| FR-031 | AI 不可用时提示用户 | 通过（test_ai.py 可用性检测测试覆盖；前端含降级态元素） |
| FR-032 | 退化为人工填筛 + 脚本抓取 | 通过（test_screening.py 降级路径测试覆盖） |
| FR-033 | 上传简历步骤可跳过 | 通过（test_webui_app.py 降级集成测试覆盖；前端含跳过简历选项） |
| FR-034 | 第二层仅硬规则核验，不执行 AI 语义相似度 | 通过（test_screening.py 仅硬规则测试覆盖） |

## 成功标准验收

| SC | 描述 | 验收方式 | 结果 |
|----|------|----------|------|
| SC-001 | 上传简历后 AI 合理时间内给出建议值 | 接口测试 + 浏览器计时 | 程序化通过（接口测试覆盖；浏览器计时待用户确认） |
| SC-002 | 确认执行后完成第一层抓取与第二层核验，分流到符合/不符合 | 端到端集成测试 | 通过（test_webui_app.py 端到端集成测试覆盖） |
| SC-003 | 符合/不符合区点感兴趣进持久区且可跳转 BOSS | 集成测试 + 浏览器 | 程序化通过（集成测试覆盖；浏览器跳转待用户确认） |
| SC-004 | 点不感兴趣后进垃圾桶且后续展示不再出现 | 集成测试 | 通过（test_webui_app.py 集成测试覆盖） |
| SC-005 | 新执行清空符合/不符合，感兴趣与垃圾桶保留 | 集成测试 | 通过（test_webui_store.py run_id 隔离测试覆盖） |
| SC-006 | AI 不可用时仍可人工填筛 + 脚本抓取 + 硬规则核验 | 降级集成测试 | 通过（test_webui_app.py 降级集成测试覆盖） |

## 安全审查

> 复查基于代码审查（T065），逐项核验 webui/ 与 scripts/ 的实际实现。
> 所有 10 项安全边界均通过代码审查验证，无违规。

### 敏感数据

| 审查项 | 结果 | 依据 |
|--------|------|------|
| 简历原文不进日志 | 通过 | webui/ 模块无任何 logging/print/logger 调用；resume.py 与 ai.py 文档明确声明 |
| 简历原文不进接口响应 | 通过 | /api/screening/resume、/api/screening/resume/suggest、/api/profiles/<id>/resume 三个端点响应只含 resume_id/suggestions/privacy_notice，不含 extracted_text |
| API Key 不明文入库 | 通过 | ai_settings 表只有 credential_ref 列（host 名），无 api_key 列；明文 Key 只存于系统凭据库（keyring） |
| API Key 不进日志 | 通过 | webui/ 无日志调用；AISecurityError 只携带安全错误码，from None 抑制 traceback 泄漏 |
| API Key 不进接口响应 | 通过 | get_ai_settings 显式 pop credential_ref（store.py:620），响应只含 endpoint_url/status/last_error_code/is_configured |

### 受控路径

| 审查项 | 结果 | 依据 |
|--------|------|------|
| 筛选运行产物路径在校验目录内 | 通过 | 第一层抓取产物写入 RESULT_DIR（默认 ~/.career-scout/job-result/），运行记录写入 webui.db |
| 区域清空不触及简历目录 | 通过 | 新 screening run 创建只 INSERT screening_runs 行，不调用 resume_service 的任何方法，不触及 RESUME_DIR |
| 清空不删除感兴趣或垃圾桶记录 | 通过 | 区域隔离通过 run_id 实现，无 DELETE 操作；profile_jobs（interested/deleted）只由 /interest 与 /reject 端点修改 |

### 展示排除与不自动投递边界

| 审查项 | 结果 | 依据 |
|--------|------|------|
| 不感兴趣排除只在展示阶段，不修改抓取结果 | 通过 | exclude_trash_jobs 是纯列表推导（screening.py:399-410），返回新列表，不写 DB，不修改原始抓取产物 |
| 无自动投递路由 | 通过 | 42 个路由中无 /api/apply、/api/submit、/api/message、/api/contact；test_workbench_api.py:388 断言 /api/jobs/<id>/apply 被拒绝 |
| 无自动联系招聘者 | 通过 | 无任何联系招聘者或发消息的路由或函数 |
| 无录用概率预测 | 通过 | 全代码库无 probabilit/predict/hiring_prob 匹配；AI 只做 parse_resume/suggest_screening_filters/rank_jds/assess_semantic_similarity（占位恒过） |
| AI 不能决定任务状态 | 通过 | assess_semantic_similarity 占位恒返回 match，不决定任务状态；分流由 partition_job 程序逻辑裁定 |

### 链接安全

| 审查项 | 结果 | 依据 |
|--------|------|------|
| 感兴趣区链接仅 HTTPS | 通过 | normalize_job_link（workbench.py:24-48）拒绝非 HTTPS；/interest 与 /reject 端点经 _save_screening_job_to_store 校验，不安全返回 400 invalid_link |
| 仅预期 BOSS 域名 | 通过 | ALLOWED_JOB_HOSTS = {www.zhipin.com, zhipin.com}；非 BOSS 域名返回空字符串被拒绝 |
| 拒绝 javascript: 协议 | 通过 | javascript: scheme != https 被拒绝；程序化验收已验证 |
| 前端 openSafe 校验 | 通过 | index.html 含 https: 与 zhipin.com 校验逻辑、window.open + noopener；程序化验收已验证 |

## 真实来源冒烟测试

命令：`python scripts/boss_cdp_raw.py --smoke-test`

结果：**阻塞（需用户在已登录专用 Chrome 上手动执行）**

### 执行记录

- 执行时间：2026-07-13
- 执行环境：Windows，无运行中的专用 Chrome（CDP 端口 9222 无人监听）
- 执行结果：失败 —— 无法连接到 127.0.0.1:9222（`WinError 10061`，目标计算机积极拒绝）
- 可追踪日志：`HTTPConnectionPool(host='127.0.0.1', port=9222): Max retries exceeded with url: /json/version (Caused by NewConnectionError: Failed to establish a new connection)`

### 阻塞原因

真实来源冒烟测试要求专用 Chrome 已通过 `python scripts/boss_cdp_raw.py --setup-chrome` 启动并在 BOSS 直聘完成登录。当前环境无运行中的专用 Chrome，无法执行。自动化测试中的模拟抓取不能替代此项。

### 解除阻塞条件

用户需在本机执行以下步骤后重跑冒烟测试：

1. `python scripts/boss_cdp_raw.py --setup-chrome`（启动专用 Chrome 并登录 BOSS）
2. `python scripts/boss_cdp_raw.py --smoke-test`（确认真实 BOSS 搜索返回有效岗位与明文薪资）
3. 将执行结果与可追踪日志补充到本节

## 未完成或受外部条件阻塞的项目

| 项目 | 状态 | 说明 |
|------|------|------|
| 第二层 AI 语义相似度框架 | 待设计 | 本次不实现，T031 只留接口契约与占位；框架设计另做 |
| 真实来源冒烟测试 | 阻塞 | 需用户在已登录专用 Chrome 上执行；当前无运行中的专用 Chrome |
| 交互式浏览器操作 | 待执行 | 点击卡片、两区切换、垃圾桶查看等需用户在浏览器中手动确认 |
