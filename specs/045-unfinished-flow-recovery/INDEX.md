# 045 Spec 索引

本目录记录“未完成流程统一找回与关闭保存”主体的需求、设计和任务版本。

## 当前版本

- 当前版本：`v2`
- 默认读取：`v2/`
- 当前版本状态：`实现完成，待真实界面走查`（2026-09-25；聚焦测试与前端全量已过，后端全量与 B104 式四路径走查待执行）
- 历史基线：目录根部平铺文档是版本框架启用前的 `v1` 历史入口，记录未完成流程进历史（B101）、关闭前保存（B102）、异常退出启动接回（B103）与分段真实回归（B104），**已实现**。
- `v2` 更新（本轮只做两件事）：
  1. 关闭确认框改为**应用内自绘**并跟随 boss/智联 × 明暗四套主题，替换 v1 的系统原生弹窗；
  2. 补上**「正在运行中关闭」**这一场景，v1 只覆盖了暂停 / 报错 / 未运行。
- 根目录平铺文档保留且只读，不作为本轮实现依据。

## 版本内容

当前版本按阶段逐步补齐以下工件，不允许跨版本混用：

- `spec.md`：需求与验收基线
- `checklists/requirements.md`：Spec 质量检查
- `plan.md`：实现设计与文件边界（Plan 阶段生成）
- `tasks.md`：执行任务与验证门禁（Tasks 阶段生成）
- `research.md`：查证证据与技术决策（Plan 阶段整理）
- `data-model.md`：数据模型（如 Plan 阶段确认需要）
- `quickstart.md`：验证指南（Plan 阶段生成）
- `contracts/`：内部契约（如 Plan 阶段确认需要）
- `changes.md`：从 `v1` 到 `v2` 的变化说明（Plan 阶段补齐）

## 后续 AI 读取与更新规则

1. 处理 045 相关需求时，先读取本文件，再完整读取当前版本目录。
2. 实施、审查和验证必须明确写明对应 `045 v2`，不得用根目录平铺文档里的原生弹窗口径覆盖当前要求。
3. `v2` 只改变关闭确认的呈现载体（应用内自绘 + 主题跟随）与「正在运行中关闭」场景；v1 已落地的落轮、启动接回、分段回归结论不在本轮变更范围内。
4. 本轮**逻辑复用、不新增机制**：收尾仍走既有通用路线，不为关闭确认另做独立存储、状态机、页面或提醒体系。
5. 网页浏览器版与源码版**不做任何关闭确认**，不得注册 `beforeunload`。
6. 同一主体后续再次改变行为时，新建下一版本并更新本索引；历史版本不得覆盖或删除。
