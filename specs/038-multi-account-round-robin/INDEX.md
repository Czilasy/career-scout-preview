# 038 Spec 索引

本目录记录“多账号轮询分摊抓取”功能的需求、设计和任务版本。

## 当前版本

- 当前版本：`v4`
- 默认读取：`v4/`
- 当前版本状态：`已冻结，待实施`；V4 修复正式 R2 运行暴露的跨分块轮询重置、账号间共享本地熔断、虚假分配留痕、重试产物覆盖和恢复后旧 JD 失败残留。
- 已实现界面基线：`v3`，状态为“已实施，待正式账号端到端验收”；V4 不修改其账号弹窗与手动清除限流标记行为。
- 已实现运行基线：`v2`，但最近正式大规模 R2 运行证明其核心多账号分摊和白箱验收仍有缺陷，不得再把历史小规模冒烟当作 V4 已通过证据。
- 历史基线：`v1`；涉及当前已落地行为时，不得把 Draft 版本当作已实现事实。
- 当前基线：`v1` 保留原 038 单版本目录中的 Spec、Plan 和 Tasks 内容；未在本次归档中改写需求语义。
- `v2` 更新：在 038 主体内增加白箱接入、账号轮询事实记录和可复核验收；`v1` 保持只读。
- `v3` 更新：在同一账号弹窗内按用户选定的紧凑清单方向重排现有内容，并允许用户手动清除账号的限流视觉标记；不改变轮询、CDP 或白箱语义。
- `v4` 更新：恢复 V2 的真实分摊目标为阻断级验收，要求 R2 轮询状态贯穿完整任务、账号级本地熔断隔离、接力账号真实请求、预留与真实终态分层留痕、尝试产物唯一和恢复状态清理。
- `v2` 历史实测：正式 WebUI 曾通过 BOSS 真实入口执行 1 个关键词 × 1 个城市、列表 2 页；R1 每账号 1 页、R2 每账号 10 个 JD。该规模没有跨越当时账号配额和多个外层详情分块，不能覆盖 V4 已核实缺陷。
- `v4` 诊断基线：最近正式 R2 任务共得到 1048 个详情岗位，实际成功来源为第一个账号 1046、第二个账号 2、其余四个账号 0；白箱累计预留 1146 个单位，并残留 16 条已被恢复成功取代的旧 JD 失败记录。
- 后续版本：当同一功能的行为、架构、验收条件或运行边界再次发生变化时，新增 `v5/` 等版本目录，并在这里更新当前版本。

## 版本内容

每个版本必须成套保存前三个文件，不允许混用不同版本；从 `v2` 开始，版本升级还必须增加 `changes.md`：

- `spec.md`：需求与验收基线
- `plan.md`：实现设计与文件边界
- `tasks.md`：执行任务与验证门禁
- `changes.md`：从上一版本到当前版本的变化说明（`v2` 及以后版本必需）

V4 另含 `research.md`、`data-model.md`、`contracts/`、`quickstart.md` 和 `checklists/requirements.md`，供实现、恢复契约与验收直接使用。

## 后续 AI 读取与更新规则

1. 处理 038 相关需求时，先读取本文件，再读取“当前版本”目录中的完整文档。
2. 如果用户提出的功能、问题或更新已经在当前 Spec 中有对应内容，先定位对应章节，向用户建议更新当前功能的下一个版本；不要直接另起一个无关 Spec。
3. 如果是当前 Spec 完全没有的新功能，才建议新建新的 Spec。
4. 如果是同一功能的新版本，复制当前版本作为新版本基线，保留旧版本，并在 `changes.md` 中记录新增、修改和废弃内容。当前 `v2` 即为示例。
5. 新版本冻结后，旧版本只读；Plan、Tasks、测试和实现必须明确对应的 Spec 版本。

## 版本判定

- 仅错别字、格式或链接修正：不改变功能版本。
- 新增或修改用户可见行为、系统架构、验收标准、数据边界或失败处理：升级版本。
- “当前版本”以本索引为准，不按文件名排序或修改时间推断。
