# 036 Spec 索引

本目录记录“Windows 桌面壳自绘标题栏与窗口交互”主体的需求、设计和任务版本。原 036 同时包含顶栏胶囊灵动岛；本轮仅迭代其中的桌面窗口交互，灵动岛既有行为不在本轮变更范围内。

## 当前版本

- 当前版本：`v2`
- 默认读取：`v2/`
- 当前版本状态：`已验收`（2026-09-14 用户明示「测试完通过了」；Windows 10 真机自测通过）
- 历史基线：目录根部平铺文档是版本框架启用前的 `v1` 历史入口，记录自绘标题栏、固定普通窗口尺寸和灵动岛能力。
- `v2` 更新：恢复 Windows 桌面壳八方向拉伸，补齐顶栏拖动还原、顶边拖动最大化，并把用户调整后的普通窗口矩形纳入可靠记忆。
- 根目录平铺文档保留且只读，不作为本轮实现依据。

## 版本内容

当前版本按阶段逐步补齐以下工件，不允许跨版本混用：

- `spec.md`：需求与验收基线
- `checklists/requirements.md`：Spec 质量检查
- `plan.md`：实现设计与文件边界（Plan 阶段生成）
- `tasks.md`：执行任务与验证门禁（Tasks 阶段生成）
- `research.md`：查证证据与技术决策（Plan 阶段整理）
- `data-model.md`：窗口状态模型（如 Plan 阶段确认需要）
- `quickstart.md`：验证指南（Plan 阶段生成）
- `contracts/`：内部契约（如 Plan 阶段确认需要）
- `changes.md`：从 `v1` 到 `v2` 的变化说明（Plan 阶段补齐）

## 后续 AI 读取与更新规则

1. 处理 036 相关需求时，先读取本文件，再完整读取当前版本目录。
2. 实施、审查和验证必须明确写明对应 `036 v2`，不得用根目录平铺文档中的“固定大小”覆盖当前要求。
3. `v2` 只改变 Windows 桌面窗口的拉伸、顶栏拖动和普通窗口记忆；灵动岛、主题视觉和 macOS 标题栏保持既有行为。
4. `packaging/desktop.py` 已超过项目 Python 文件规模红线；任何行为保持式拆分必须单独立项、先于本功能实施，不能混进 `036 v2`。
5. 同一主体后续再次改变行为时，新建下一版本并更新本索引；历史版本不得覆盖或删除。
