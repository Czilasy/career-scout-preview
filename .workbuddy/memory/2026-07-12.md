# 2026-07-12

## AI 求职工作台 前端优化
- 用户认为 webui/index.html（单文件暗色后台）太丑，征求优化意见。
- 给出针对性 critique：卡片锁死 300px 高、桌面单列浪费空间、裸 GitHub 灰无品牌、零图标、操作区拥挤、设置面板高密度、薪资绿与成功态撞色。
- 用户选择"先只给设计稿不动代码"。已产出设计稿 `docs/design-spec.html`（深色+浅色双主题，含配色/字体/组件/框架/岗位卡片改前改后/空态骨架屏），仅视觉规范，未改业务代码。
- 落地顺序建议：A档(卡片自适应高度+多列网格+操作分层+图标+hover) → B档(顶部指标条+设置区分组+空态骨架屏) → C档(深/浅主题切换+快捷键+匹配度标签)。
- 待用户确认风格方向后，再决定是否落地改进版 index.html（app.py 不动）。

## v2 设计稿（极简高对比）
- 用户认为 v1（冷蓝紫暗色 SaaS 风）太丑，要求换版本。经确认方向选"极简高对比"。
- 已产出 `docs/design-spec-v2.html`：近黑白、发丝描边、等宽字体标签、反白主按钮、零渐变零阴影、状态用 8px 圆点、带黑白反相切换。仍只改视觉规范，未动业务代码。
- 用户审美偏好：不喜欢 v1 那种"暖软 SaaS"风；倾向极简/高对比/近黑白/极客感。后续若做 v3 或落地，默认走这个方向。

## 备用页面 index-v2.html（落地实现）
- 用户确认 v2 设计稿不错，要求先做备用页面、不要直接替换原 index.html。
- 已新建 `webui/index-v2.html`：功能逻辑与原版完全一致（同接口/字段/交互），视觉套用 v2 极简高对比，并落实 A/B/C 改进：多列网格(repeat auto-fill minmax 340px)、卡片自适应高度、操作分层+「更多」菜单(不感兴趣原因/不感兴趣/删除)、等宽标签、反白主按钮、骨架屏、空态引导(∅)、黑白反相切换、顶部指标(FOUND/LIKE/SENT)、连接状态药丸。
- 在 `webui/app.py` 新增 `/v2` 路由(send_from_directory 指向 index-v2.html)，原 `/` 与 index.html 完全不动。
- 查看方式：直接浏览器打开文件看视觉(空态)；或运行后端后访问 /v2 看实时数据。尚未启动后端验证（环境依赖/BOSS 登录未确认）。
- 待用户确认后，再决定是否用 index-v2.html 替换 index.html。
