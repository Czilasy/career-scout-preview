# BOSS直聘抓取流程性能优化——对照实验执行报告

> **文档状态**：设计完成，待执行
> **创建时间**：2026-07-26
> **实验对象**：东莞 8 关键词抓取流程（2026-07-26 12:00–16:24，4 小时 24 分钟）

---

## 一、问题陈述

### 1.1 现象
2026-07-26 一次完整抓取流程耗时 **4 小时 24 分钟**：
- 抓取范围：东莞，8 个关键词（AI应用开发、多智能体开发、LLM应用开发、智能体开发、智能体编排、AI、AI专员、低代码）
- 数据规模：2364 条原始岗位，去重后 1558 条
- 流程：12:00 开始列表页抓取 → 16:24 结果存档完成

### 1.2 时间分布（从数据库 `screening_results.created_at` 反推）

| 阶段 | 时间段 | 耗时 | 占比 | 瓶颈定位 |
|---|---|---|---|---|
| 列表页抓取（8 关键词）| 12:00 → 12:56 | 56 分钟 | 21% | 中 |
| Stage A 粗筛（1558→1152 kept）| 12:56 → 13:05 | ~9 分钟 | 5% | 低 |
| 抓 JD（1152 条）| 13:05 → 15:14 | **134 分钟** | **43%** | **最高** |
| Stage B 精筛（882 条）| 15:14 → 16:24 | **70 分钟** | **27%** | **次高** |
| 结果存档 | 16:24:13 | <1 秒 | 0% | — |

### 1.3 数据缺口说明
Stage A 后 kept=1152，但进入 Stage B 的只有 882 条，差值 270 条的流失原因未在本次实验中追溯（可能是去重、JD 抓取失败被剔除、或 Stage A 边界条件）。这 270 条不影响本次性能实验结论，但需在后续大样本实验中查清。

---

## 二、实验目标

### 2.1 主目标
验证以下两类优化参数是否安全，并固化新默认值：
1. **抓 JD 三件套**优化：缩短 43% 时间（134 分钟 → 预期 60-80 分钟）
2. **AI 并发与批量**优化：缩短 27% 时间（70 分钟 → 预期 25-35 分钟）

### 2.2 非目标（本次不做）
- 极限测试（把单字段推到崩溃点）：会引入新变量，污染对照结论，延后单独做
- `screen_batch_size` 优化：当前 50 已较优，且粗筛仅占 5% 时间，收益小
- `pages` / `inter_combo_delay` 优化：非性能参数
- Stage A 粗筛前置过滤策略：属策略优化，不属参数优化

### 2.3 成功标准
- 4 组对照实验全部跑完，无未捕获异常
- 决策矩阵能给出明确的"固化/回退/再测"结论
- 总耗时压缩预期：5 小时 → 2 小时以内（基于小样本推算）

---

## 三、实验环境

### 3.1 系统环境
- **应用**：BOSS直聘职位抓取与筛选工具（本地单用户桌面应用）
- **技术栈**：Python + Flask + Vue + SQLite + CDP 浏览器自动化
- **代码位置**：`D:\项目\boss\webui\`
- **运行数据位置**：`~/.career-scout\`

### 3.2 AI 端点配置
- **端点**：`https://opencode.ai/zen/v1/chat/completions`
- **模型**：`deepseek-v4-flash`（付费版，按量计费）
- **单价**：输入 $0.14 / 1M tokens，输出 $0.28 / 1M tokens
- **官方限流**：[opencode.ai/docs/zen](https://opencode.ai/docs/zen) **未明确 RPM/TPM 上限**，仅月度金额上限
- **实测速率**：约 8-9 RPM（每条 1500-3000 输入 / 800-9000 输出 tokens），远未触顶

### 3.3 当前高级执行设置（基线配置）
```json
{
  "pages": 10,
  "inter_combo_delay": 10.0,
  "detail_batch_size": 6,
  "detail_interval": 4,
  "detail_reset_every": 4,
  "detail_batch_cooldown": 15,
  "screen_batch_size": 50,
  "screen_concurrency": 3,
  "match_batch_size": 4,
  "match_concurrency": 2
}
```

---

## 四、可优化字段分类

### 4.1 字段清单与角色

| 字段 | 当前值 | 范围 | 角色 | 本次是否作为变量 |
|---|---|---|---|---|
| `pages` | 10 | 1-10 | 样本量 | 否（固定为 3）|
| `inter_combo_delay` | 10s | — | 组合间隔 | 否（本次不优化）|
| `detail_batch_size` | 6 | 1-15 | 抓 JD 每批数量 | **是（组 2/4 变量）** |
| `detail_interval` | 4s | 2-15 | 抓 JD 单岗间隔 | **是（组 2/4 变量）** |
| `detail_reset_every` | 4 | — | 每 N 批重置 | 否（保持 4 不变）|
| `detail_batch_cooldown` | 15s | 5-60 | 抓 JD 批次冷却 | **是（组 2/4 变量）** |
| `screen_batch_size` | 50 | 1-100 | 粗筛每批数量 | 否（保持 50）|
| `screen_concurrency` | 3 | 1-10 | 粗筛并发数 | 否（**本次不测，4 组全保持 3**，见 5.5 节）|
| `match_batch_size` | 4 | 1-20 | 精筛每批数量 | **是（组 3/4 变量）** |
| `match_concurrency` | 2 | 1-10 | 精筛并发数 | **否（固定常量 = 6）** |

### 4.2 固定常量说明
**`match_concurrency = 6`** 是本次实验的**固定常量**，不是实验变量。

设置理由：
- 用户指定为缩短 Stage B 单组耗时的工具值
- 取前端上限 10 的 60%，留出安全余量
- 所有 4 组统一使用此值，确保 Stage B 阶段在组间可比

**重要**：本次实验不验证 `match_concurrency=6` 是否安全（这是用户给定的常量），只验证其他变量在 `match_concurrency=6` 固定下的表现。

---

## 五、对照实验设计

### 5.1 共同样本（保证对照有效性）
- **关键词**：「AI应用开发」1 个
- **城市**：东莞 1 个
- **列表页**：`pages=3`（约 90 条岗位）
- **其他环境变量**：同一台机器、同一网络、同一 AI 端点、同一节点

### 5.2 四组对照配置

| 组号 | 名称 | detail_batch_size | detail_interval | detail_batch_cooldown | screen_concurrency | screen_batch_size | match_batch_size | match_concurrency |
|---|---|---|---|---|---|---|---|---|
| **1** | 基线组 | 6 | 4 | 15 | 3 | 50 | 4 | **6** |
| **2** | 抓JD组 | 15 | 2 | 5 | 3 | 50 | 4 | **6** |
| **3** | AI精筛组 | 6 | 4 | 15 | 3 | 50 | 8 | **6** |
| **4** | 全极限组 | 15 | 2 | 5 | 3 | 50 | 8 | **6** |

### 5.3 各组用途说明

#### 组 1：基线组
- **目的**：建立对照组的基准线
- **配置**：所有字段保持当前 `advanced_settings.json` 值（除 `match_concurrency` 固定为 6）
- **意义**：所有其他组的性能/稳定性/质量指标都与本组对比

#### 组 2：抓JD组
- **目的**：验证抓 JD 三件套极限值是否触发反爬
- **变量**：`detail_batch_size` 6→15、`detail_interval` 4→2、`detail_batch_cooldown` 15→5（**复合变量**，承认归因限制）
- **预期**：抓 JD 耗时从 ~5 分钟降到 ~2 分钟
- **风险**：可能触发 BOSS code:37 反爬

#### 组 3：AI精筛组
- **目的**：验证 `match_batch_size=8` 是否触发 429 + 质量是否退化
- **变量**：`match_batch_size` 4→8（**单一变量**）
- **预期**：Stage B 耗时降低（批数减半）
- **风险**：`match_batch_size=8` 可能让 AI 信息丢失（精筛请求体大）；可能触发 opencode.ai 429
- **不测 `screen_concurrency` 的原因**：90 条样本只产生约 2 批粗筛任务，`screen_concurrency=3` 和 `=10` 实际行为完全一样（都只跑 1 轮），无法形成对照。放弃本次测，让 Stage A 阶段在 4 组间成为恒定量（见 5.5 节）

#### 组 4：全极限组
- **目的**：验证两个维度同时极限化是否产生叠加副作用
- **配置**：组 2 + 组 3 的所有变量同时拉满
- **意义**：如果组 2 和组 3 都安全，但组 4 出问题，说明两维度有交互作用

### 5.4 单一变量原则的诚实说明
**本实验的变量结构**：
- 组 2：复合变量（detail 三件套，3 字段同时改）
- 组 3：**单一变量**（只改 `match_batch_size`）
- 组 4：组 2 + 组 3 的叠加

组 3 已收敛为单一变量，归因干净。组 2 仍是复合变量，如果组 2 出问题，无法精确归因到 `detail_batch_size` / `detail_interval` / `detail_batch_cooldown` 中的哪一个。

**为什么接受组 2 的复合变量**：
- 拆成 3 个子组实验膨胀到 6+ 组，时间成本过高
- 用户明确要求"不要混淆""先稳定""最稳妥"，故接受复合变量
- 折中方案：每个字段在组内的取值是"当前值 vs 极限值"两档，组内可做粗略归因

### 5.5 Stage A 恒定量声明（新增）
**因样本量不足以测 `screen_concurrency`，Stage A 阶段在 4 组间配置完全一致**（`screen_concurrency=3`、`screen_batch_size=50` 不变），作为本次实验的恒定量。

这带来一个好处：对照组的归因更干净——只有抓 JD 和 Stage B 是变量，Stage A 耗时组间差异应在噪声范围内。

**环境干扰判定阈值**：若实验中 Stage A 耗时组间差异 >10%，视为环境干扰（如网络抖动、CPU 抢占），结论作废重测。

---

## 六、指标体系

### 6.1 性能指标
| 指标 | 说明 | 单位 |
|---|---|---|
| 总耗时 | 从开始抓取到结果存档完成 | 秒 |
| 列表页耗时 | 抓取列表页阶段耗时 | 秒 |
| 抓 JD 耗时 | 抓取职位详情阶段耗时 | 秒 |
| Stage A 耗时 | 粗筛阶段耗时 | 秒 |
| Stage B 耗时 | 精筛阶段耗时 | 秒 |
| 单岗 JD 耗时 | 抓 JD 耗时 / 岗位数 | 秒/条 |
| 单批 AI 耗时 | AI 调用耗时 / 批次数 | 秒/批 |

### 6.2 稳定性指标
| 指标 | 说明 | 单位 |
|---|---|---|
| 429 次数 | AI 端点返回 429 的次数 | 次 |
| code:37 次数 | BOSS 返回反爬错误码 37 的次数 | 次 |
| 节点切换次数 | clash-node-switcher 触发切换的次数 | 次 |
| JD 失败数 | 抓 JD 失败的岗位数 | 条 |

### 6.3 质量指标
| 指标 | 说明 | 单位 |
|---|---|---|
| match 数 | AI 判定匹配的岗位数 | 条 |
| mismatch 数 | AI 判定不匹配的岗位数 | 条 |
| uncertain 数 | AI 拿不准的岗位数 | 条 |
| dropped 数 | Stage A 粗筛丢弃的岗位数 | 条 |
| 配对差异 | 与基线组的 match 数差异 | 条 |

### 6.4 成本指标
| 指标 | 说明 | 单位 |
|---|---|---|
| 总 input tokens | 所有 AI 调用的输入 token 总和 | tokens |
| 总 output tokens | 所有 AI 调用的输出 token 总和 | tokens |
| 总成本 | 按 $0.14+0.28 per 1M 估算 | USD |
| 单岗成本 | 总成本 / 岗位数 | USD/条 |

---

## 七、量化阈值

### 7.1 稳定性判定阈值
| 指标 | "无问题" | "边界"（视为噪声）| "有问题" |
|---|---|---|---|
| 429 次数 | 0 | 1 | ≥2 |
| code:37 次数 | ≤1 | 2 | ≥3 |
| 节点切换次数 | 0 | 1 | ≥2 |
| JD 失败数 | ≤2 | 3-5 | ≥6 |

### 7.2 质量判定阈值
| 指标 | "无退化" | "边界" | "退化" |
|---|---|---|---|
| match 分布差异 | ≤4 条 | 5-7 条且变化率 <25% | ≥5 条**且**变化率 ≥25%，或 ≥8 条任意变化率 |
| mismatch 分布差异 | ≤5 条 | 6-10 条 | ≥11 条 |

**说明**：pages=3 样本下 match 数约 20-40 条，5 条差异占 12-25%，仍可能被 AI 随机性解释。加"变化率 ≥25%"过滤小样本噪声；同时保留"≥8 条任意变化率"兜底，防止"基线 40 条、对照组 32 条"这种绝对差异大但变化率不到 25% 的退化被漏判。

### 7.3 决策矩阵
| 组 2（抓JD）结果 | 组 3（AI精筛）结果 | 组 4（全极限）结果 | 决策 |
|---|---|---|---|
| 无反爬 | 无 429 且无退化 | 无问题 | **固化组 4 全部参数为新默认值**（detail 三件套 + match_batch_size=8）|
| 无反爬 | 有 429 或退化 | 有 429 或退化 | **只固化组 2 抓 JD 参数**，match_batch_size 保持 4 |
| 有反爬 | 无 429 且无退化 | 有反爬 | **只固化组 3 的 match_batch_size=8**，抓 JD 保持当前 |
| 有反爬 | 有 429 或退化 | 有问题 | **两端都保守**，回退当前值，转向策略优化 |
| 无反爬 | 无 429 且无退化 | 有问题 | **固化组 2 + 组 3 各自参数**，但不组合使用（两维度有交互）|

**注**：组 3 现在只测 `match_batch_size`，所以"组 3 有问题"= 429 触发或质量退化，归因明确到 `match_batch_size=8`。

---

## 八、实验执行方式

### 8.1 执行模式
**串行执行**（不并行），原因：
1. BOSS 反爬配额按 IP 计算，并行时组间互相污染，无法归因
2. opencode.ai 限流是账号级，并行时高并发组会拖累基线组
3. Clash 节点切换是进程级全局动作，一组触发切换会污染其他组的网络环境

### 8.2 执行顺序
组 1 → 组 2 → 组 3 → 组 4，**固定顺序不随机化**：
- 组 1 必须最先跑，建立基线
- 组 4 必须最后跑，避免触发反爬后影响后续组
- 组 2 和组 3 顺序可互换，本次按"抓 JD 先于 AI"排序

### 8.3 自动化脚本设计（待编写）
脚本职责：
1. 备份当前 `advanced_settings.json`
2. 按组号写入新配置
3. 调用 `pipeline_exec` 入口函数（不通过前端，避免 UI 干扰）
4. 跑完记录全部指标到 `experiment_results.json`
5. 4 组串行执行
6. 执行完毕自动恢复原 `advanced_settings.json`
7. 输出对比表格（Markdown 格式）

### 8.4 预期耗时
- 单组约 10-15 分钟（pages=3、约 90 条岗位、match_concurrency=6 提速后）
- 4 组串行约 **40-60 分钟**

**耗时估算依据**：
- 列表页 3 页约 1-2 分钟
- 抓 JD：60-70 条 ÷ 6 条/批 × 39 秒/批 ≈ 6-8 分钟（基线组）；极限组约 3-4 分钟
- Stage A：约 1-2 分钟（恒定量，组间差异应 <10%）
- Stage B：约 2-3 分钟（match_concurrency=6 提速后）

---

## 九、已知风险与未知项

### 9.1 风险
| 风险 | 概率 | 影响 | 缓解措施 |
|---|---|---|---|
| 组 2/4 触发 BOSS 反爬 | 中 | 抓 JD 失败、IP 被限 | 观察 code:37 频率，≥3 次立即终止该组 |
| 组 3/4 触发 AI 429 | 低 | AI 调用失败、节点切换 | 已有 clash-node-switcher 兜底 |
| 组 3/4 质量退化 | 中 | match 数下降 ≥5 条且变化率 ≥25%，或 ≥8 条任意变化率 | 比较 match 分布，退化则回退 |
| 样本量不足 | 高 | 低频问题无法暴露 | 结论标注"小样本"，后续大样本验证 |
| 时段混淆 | 低 | 429 数据被时段污染 | 40-60 分钟内时段影响可忽略 |

### 9.2 未知项
1. **Clash 节点切换是否启用**：若启用，组 3/4 的 429 会被节点切换"吸收"，无法反映真实限流。需在实验前确认。
   - **用户确认（2026-07-26）**：已启用，但不影响实验进行。
   - **备注**：启用状态下，429 触发会被 clash-node-switcher 主动切换节点避让，因此实验中观测到的 429 次数可能**低估**真实限流频率。解读组 3/4 的 429 数据时需注意：429=0 不等于"端点无限流"，只等于"节点切换成功兜底"。但仍可据此判断"当前配置在节点切换兜底下是否可用"。

2. **`detail_reset_every` 字段语义**：本次保持 4 不变，但极限测试阶段需查清。
   - **用户确认（2026-07-26）**：极限测试不包含在本次实验内，不影响本次实验。

3. **opencode.ai 月度余额**：若余额接近上限，组 4 高并发可能因余额耗尽而失败，与限流混淆。需在实验前确认余额。
   - **用户确认（2026-07-26）**：月度余额充足，不影响实验。

4. **270 条岗位流失原因**：Stage A 后 1152 → Stage B 882 的差值未追溯，不影响本次实验但需后续查清。
   - **用户确认（2026-07-26）**：属另一个独立问题（部分岗位抓不到 JD），不影响本次性能实验。本次先解决时间问题，270 条流失原因留待后续单独追溯。

### 9.3 样本量风险声明
**本次实验样本量约 90 条岗位**（pages=3），存在以下限制：
- 90 条的 10% = 9 条，AI 随机性可能造成 ±5 条波动，质量指标已加"变化率 ≥25%"过滤小样本噪声
- 低频问题（如 1% 概率的反爬触发）无法暴露
- `screen_concurrency` 因样本不足无法测（需 500+ 条），本次放弃
- **结论适用范围**：本次实验只验证"参数调整不退化"，不验证"极限边界"。极限边界需后续大样本实验单独测。

---

## 十、后续路径

### 10.1 本次实验后
1. 根据决策矩阵固化新参数到 `advanced_settings.json`
2. 用新参数跑一次完整东莞 8 关键词大样本实验（约 2-3 小时）
3. 验证 5 小时能压缩到多少

### 10.2 大样本验证通过后
1. 启动极限测试，找每个字段的天花板（含本次未测的 `screen_concurrency`）
2. 单独设计 `screen_concurrency` 的大样本实验（需 500+ 条岗位才能测出并发差异）
3. 考虑 Stage A 粗筛前置过滤策略优化（减少送进抓 JD 的岗位数）

### 10.3 长期优化方向
1. 抓 JD 阶段引入持久化 tab pool（3-5 tabs 并行抓取）
2. AI 调用引入请求合并（多个岗位合并成单次请求）
3. 列表页抓取引入多关键词并行（当前是串行 8 关键词）

---

## 十一、对照词表（用于对话快速指代）

| 类别 | 对照词 | 含义 |
|---|---|---|
| 实验组 | 基线组 / 抓JD组 / AI精筛组 / 全极限组 | 组 1 / 2 / 3 / 4 |
| 字段维度 | 抓JD三件套 | detail_batch_size + detail_interval + detail_batch_cooldown |
| 字段维度 | AI精筛变量 | match_batch_size（单一变量）|
| 字段维度 | 固定常量 | match_concurrency=6 + screen_concurrency=3（非实验变量）|
| 字段维度 | Stage A 恒定量 | screen_concurrency=3 + screen_batch_size=50（4 组一致，环境干扰判定用）|
| 性能指标 | 总耗时 / 阶段耗时 / 单岗JD耗时 / 单批AI耗时 | 性能维度 |
| 稳定性指标 | 429率 / 反爬率 / 节点切换次数 / JD失败数 | 稳定性维度 |
| 质量指标 | match分布 / 配对差异 / 质量退化 | 质量维度 |
| 成本指标 | 总tokens / 总成本 / 单岗成本 | 成本维度 |
| 结论状态 | 退化 / 持平 / 改善 | 与基线组对比的结果 |
| 风险事件 | code:37 / 429 / 节点切换超时 / OOM | 需记录的异常 |

---

## 十二、给执行者的提示词

执行实验时，按以下提示词驱动：

```
实验目标：验证 BOSS直聘抓取流程的高级执行参数优化方案，目标把 5 小时抓取压缩到 2 小时内。

当前阶段：执行 4 组对照实验，串行进行。

执行步骤：
1. 备份 ~/.career-scout\webui\advanced_settings.json
2. 按组号顺序执行（组1→组2→组3→组4），每组：
   a. 写入该组的 advanced_settings.json（match_concurrency 固定为 6，screen_concurrency 全组固定为 3）
   b. 调用 pipeline_exec 入口，关键词="AI应用开发"，城市="东莞"，pages=3
   c. 记录性能/稳定性/质量/成本四类指标到 experiment_results.json
3. 4 组跑完，恢复原 advanced_settings.json
4. 按决策矩阵（第七节）得出结论
5. 输出对比表格

注意事项：
- 串行执行，禁止并行（避免反爬配额和 AI 限流的组间污染）
- 组 2/4 观察 code:37，≥3 次立即终止该组
- 组 3/4 观察 429，≥2 次记录但不终止（有节点切换兜底）
- 质量指标对比基线组，match 差异 ≥5 条且变化率 ≥25%，或 ≥8 条任意变化率，视为退化
- Stage A 耗时组间差异 >10% 视为环境干扰，结论作废重测
- 实验完成后必须恢复原配置，避免影响生产使用
```

---

**报告结束。待执行。**
