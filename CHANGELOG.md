# 更新日志

本文件记录 Career Scout 的所有重要变更。格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [1.9.4] - 2026-09-21

增加：
- 可以保存常用搜索配置，下次一键复用

优化：
- 常用配置入口与保存弹窗更紧凑
- 使用或保存常用配置后，灵动岛及时提示

修复：
- 简历分析时灵动岛不再出现空白胶囊
- 保存配置弹窗居中显示，保持 Boss 配色

## [1.9.3] - 2026-09-15

增加：
- 上次没筛完的抓取，下次打开提醒一次，之后不再打扰
- 过旧的零散抓取记录自动清理，清理前先备份

优化：
- 未筛选结果的待筛选标签回到左边
- 未筛选结果的按钮更小更顺手
- 灵动岛点击更柔和，文字不再跟着缩

修复：
- 没跑过的流程不再自己冒出来，也不会反复出现
- 删掉一轮会连它的记录一起清干净，不再白占空间

## [1.9.2] - 2026-09-14

增加：
- 窗口四边和四角都能拖动调整大小
- 按住顶栏就能拖动窗口，拖到屏幕顶端自动最大化
- 最大化窗口从顶栏往下拖就能还原

修复：
- 调好窗口大小和位置后重启，窗口保持上次的样子

## [1.9.1] - 2026-09-13

修复：
- 切换步骤、查看历史或刷新后不再丢现场
- 灵动岛点击后回到任务真实所在步骤
- 不填城市也能正常开始抓取
- 补抓岗位时不再把人带离结果页
- 继续任务时浏览器打不开会自动重连

优化：
- 暂停与结束保存的选择框更轻巧

## [1.9.0] - 2026-09-11

修复：
- 偶发抓取失败会自动重试一次。
- 抓取结束后完成数量不再显示错误。
- 连不上浏览器不再显示为未知错误。
- 搜不到岗位时不再误报为失败，如实显示未搜到岗位。
- 只失败一个组合时不再误报为无法确认。
优化：
- 抓取失败只显示数量，悬停可看原因。

## [1.8.6] - 2026-09-11

修复：
- 智联筛选不再因抓取意外中断而莫名丢一大批岗位进待确认。
- 抓取中断后剩余岗位会自动补抓一轮，能救回来的都会救回来。
- 没抓完成的岗位如实显示为「详情抓取中断，可补抓」，不再是说不清原因的提示。
优化：
- 打开页面后平台滑块会自动跟上一次结果的平台，不用再手动切换一次配色才一致。

## [1.8.5] - 2026-09-10

修复：
- 重抓完成后只剩少量岗位待确认时，不再被误报为执行失败。
- 重抓结果会如实显示完成了多少、还剩多少没处理。
- 查看历史轮次时不再先跳到最新一轮，连续切换也能稳定停在所选轮次。

## [1.8.4] - 2026-09-10

增加：
- 简历画像会记录公司、任职区间和毕业时间等可靠信息。

优化：
- 系统会根据任职时间合并计算累计工作年限，让精筛判断更有依据。
- 求职画像与简历事实冲突时会优先按当前画像判断，并提示双休、大小周和 996 等制度信息供核对。

## [1.8.3] - 2026-09-09

增加：
- 抓取过程中可以放弃本轮并回到简历上传步骤，已保存的历史结果仍可查看。

优化：
- 继续任务前会重新确认浏览器和账号状态，恢复抓取更可靠。
- 详情补抓可以单独停止，已经得到的结果会保留。

修复：
- 可恢复的暂停、登录失效和浏览器异常不再被误显示为失败，按提示处理后可以继续。
- 重抓待确认岗位时会正确沿用原流程的登录状态，启动失败也不会留下无法操作的任务。
- 待确认岗位现在可以从结果列表标题直接重抓，历史轮次中的待确认岗位也能沿用原轮次继续处理。
- 从历史轮次回到最新时不再闪现数字全为 0 的空白结果页。

## [1.8.2] - 2026-09-05

增加：
- 任务完成状态现在区分完整成功、空结果、部分完成、失败、无法确认和中断。

优化：
- 任务进度、历史结果和开发者报告统一显示同一完成结论。
- 更新提示只展示本次更新最重要的内容，阅读更新信息更清晰。

修复：
- 多账号详情抓取现在会按各账号配额真实分摊，限流接力与暂停恢复会准确续跑并清理已恢复岗位的旧失败记录。
- 空结果、后台提交失败和完成证据缺失不再被误报为成功。

## [1.8.1] - 2026-09-04

增加：
- 灵动岛升级为通知中心，可查看任务进度、完成结果和异常状态
- 多个账号可以一起抓，账号簿支持选择参与轮询的账号并设置抓取数量，限流账号会自动由其他账号接力

优化：
- 所有任务提示统一收拢到灵动岛，投递提醒与全局通知各自清晰展示

修复：
- 完成一轮重刷后点击“开始新一轮”可直接进入下一轮
- 应用内更新提示通过国内镜像发现新版本时缺少版本介绍的问题
- 灵动岛状态、提醒角标和边界显示异常的问题得到修复

## [1.7.10] - 2026-09-02

修复：
- 应用内更新检查现在能真正走国内镜像，国内用户能稳定收到更新提示
- 检查更新遇到网络问题或限速时，不再误显示「已是最新版本」，会明确提示检查失败可重试
- 镜像可达但版本滞后时会自动回退 GitHub 复核，不会被旧 manifest 锁死老版本
- 安装包构建时自动注入更新镜像地址，旧客户端从此能稳定走镜像通道
