# 更新日志

本文件记录 Career Scout 的所有重要变更。格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [2.6.0] - 2026-08-07

### 新增

- **结果页平台筛选**：查看结果头部嵌入「全部 / 智联 / BOSS」小滑块，默认全部；全部视图合并展示双平台最近结果，单平台视图按来源过滤。
- **岗位来源徽章**：左侧岗位卡片与右侧详情均显示平台来源徽章（BOSS 浅绿、智联深蓝），并展示投递状态（已投/已投递）。
- **徽章跟随岗位名**：平台与投递徽章内联在岗位名称后方同一行，后续新增展示标记一律跟在岗位名之后。
- **统一指南针图标**：网页 favicon、Windows EXE 文件/窗口图标、macOS .app 图标全部换成与顶栏一致的指南针。

### 变更

- 移除结果页头部「匹配优先」文字标签与「已加载 N」计数，头部仅保留岗位数与平台滑块。
- 「开始 AI 筛选」按钮缩小一号并留出右边距；折叠卡片箭头缩小；高级执行设置数字输入框改为紧凑定宽；求职画像输入框改为多行展示并去除拖拽手柄。

### 修复

- **风控误判**：修复 BOSS 未封号时「开始筛选」误报 IP 级风控拦截的问题。连续空页在 API 正常应答（totalCount/hasMore 明确）时判定为该搜索条件无职位（非风控）；详情页限流识别改用更严格关键词，避免 JD 正文裸词误判。
- **进度条卡 9x%**：广泛抓取完成后进度条冻结在最后一帧的问题；终态与 paused 一样把显示值校正到真实锚点。

## [2.5.0] - 2026-08-07

### 新增

- **macOS 桌面版**：GitHub Actions 自动构建，支持 Apple Silicon（M 系列）Mac。
- **应用内更新**：启动自动检查新版本，顶栏提示、应用内下载（进度条 + SHA256 强制校验），一键重启完成更新。
- **顶栏 GitHub 按钮**：一键跳转项目仓库。

### 变更

- 账号弹窗改版：「打开 BOSS / 智联窗口」合并为单个「打开浏览器」；操作区右对齐。
- Windows 启动 Chrome 不再闪 CMD 黑窗。
- 移除「页面与当前后端不一致」运行时拦截；源码模式启动时自动重建前端。

## [2.4.0] - 2026-08-06

### 新增

- **Windows 桌面版（EXE）**：单个 EXE 文件下载即用，无需安装 Python/Node；pywebview 桌面窗口复用现有 Web 工作台界面，支持窗口缩放、状态记忆与单实例；抓取任务在应用内直接执行。
- **桌面版环境检查适配**：Python 依赖显示为内置运行时；新增 WebView2 运行时检查项；其余检查项与源码版一致。
- **可配置默认窗口尺寸**：`~/.career-scout/desktop_window.json` 可自定义默认窗口大小（默认 1366×768），无需重新打包。
- **多平台架构**：接入智联招聘，与 BOSS 直聘共用统一岗位模型；顶栏一键切换平台，搜索条件、筛选字段与城市选项按平台规则独立加载。
- **平台隔离的结果展示**：岗位入库时冻结来源平台身份，查看结果按平台隔离，BOSS 与智联结果互不混入。
- **岗位生命周期**：岗位详情支持标记已读、已投递、已荒废及状态纠正；已投递记录真实投递时间（可修正），跟进作为独立时间事件记录。
- **投递过期提醒**：已投递满 30 天（精确 30 × 24 小时）且无跟进的岗位进入顶部导航提醒徽标与提醒抽屉，按最长未活动时间排序，单次最多 100 条；BOSS 与智联岗位统一提醒，不按平台过滤。
- **按需 AI 行动建议**：针对单个逾期岗位请求 AI 建议，输出仅限"跟进"或"复核"；AI 不可用、失败或缺少 JD 时返回规则式兜底建议。
- **客观生命周期事件轨迹**：所有状态与时间操作追加不可覆盖事件，与"感兴趣/不感兴趣"偏好反馈分离。
- **浏览器登录态按平台隔离**：BOSS 与智联使用独立登录空间。

### 变更

- 开源协议由 MIT 调整为 Apache License 2.0。
- README 重写：覆盖多平台能力、反馈闭环功能与界面截图。
- 新增 CONTRIBUTING.md、CHANGELOG.md、SECURITY.md。

## [2.3.0] - 2026-07-30

### 已具备的能力

- BOSS 直聘 CDP 抓取（CLI + Web 工作台），支持断点续抓。
- 简历驱动的两层筛选（硬规则 + AI 语义评估），AI 不可用时降级。
- 感兴趣/垃圾桶持久区与匹配/不匹配/待确认/已筛除分类结果。
- 断点续跑、浏览器 A/B 账号、高级设置与调优实验框架、历史任务恢复。
- `job_summary` 岗位市场摘要与求职材料优化提示词。
