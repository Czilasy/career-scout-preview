# 更新日志

本文件记录 Career Scout 的所有重要变更。格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [1.7.10] - 2026-08-11

修复：
- AI 精筛失败批次不再末尾重复重试，未判岗位直接进入待确认
- 按“元/天”计薪的实习或兼职岗位不再被整批误判为疑似骗局
- AI 服务限流或临时故障自动重试，连续 3 次失败才报错
- 智联详情页瞬时加载失败不再误判平台封禁，批内其它岗位继续抓取
- 暂停的智联筛选继续时恢复冻结账号身份，缺失身份时保持暂停
- 任务进度不再每秒重复播报，只在状态、阶段或关键计数变化时播报
- 同一时间只允许一个抓取或 AI 筛选任务占用浏览器，避免任务互相抢占

增加：
- 新增历史轮次入口，可回看每轮 BOSS 与智联结果、完整画像并导出
- 重新上传简历或开始新一轮时旧结果自动归档保留，不再删除
- 顶栏设置菜单收纳 AI 设置、浏览器账号、环境检查、更新检查与 GitHub 仓库
- 第二步新增“开始筛选并 AI 优化”一键入口，确认筛选条件后自动完成抓取与 AI 筛选
- 一键启动与 AI 筛选前校验求职画像至少 10 个字，不足时提示补齐
- 删除账号与解除冷却改用应用内确认弹窗
- 新增 CI 质量门禁与 GitHub Release 发布说明模板

优化：
- 薪资筛选按已选区间硬性执行，不再出现同一岗位结果互相矛盾的判断
- AI 精筛进度显示 100% 后不再长时间停留在运行中
- 顶栏结果平台名与已判定数保持一致，历史轮次跟随所选轮次
- 环境检查结果一次性展示，不再逐项点亮制造假进度
- 页面标题跟随平台与页面状态，不再固定显示 BOSS 工作台
- 移除远程字体依赖，离线与无网环境样式正常
- 启动脚本只关闭 Career Scout 旧进程，并等待服务就绪后再打开浏览器

## [2.8.5] - 2026-08-09

修复：
- 修复应用内更新下载完成后未重启，再次安装报文件已存在的问题

优化：
- 关闭 GitHub 更新检查 24 小时缓存，启动与手动检查都实时查询最新版本

## [2.8.4] - 2026-08-08

修复：
- 修复抓取组合进度与实际执行状态不一致的问题
- 切换平台前提示未进行 AI 筛选的抓取任务不会保存

优化：

## [2.8.3] - 2026-08-08

修复：
- 修复结果页浮层定位，统一收藏与提醒抽屉动画
- 修复网页版仍会弹出最新版更新提示的问题

增加：
- 界面加入全局微动画，主题切换、弹窗、抽屉、任务进度条更柔和

优化：
- 网页版不再提示应用内更新，源码模式更新代码后重启即可

## [2.8.2] - 2026-08-08

修复：
- 修复应用内更新后旧版安装文件残留的问题，新版自动替换为带版本号的文件
- 修复应用内更新重启可能出现的退出提示误报

优化：
- 更新替换脚本改为无窗口执行，不再弹出黑色命令窗口

## [2.8.1] - 2026-08-08

### 变更

- 修复应用内更新重启失败、黑窗与文件名不更新

## [2.8.0] - 2026-08-08

### 新增

- **智联抓 JD 完整遵循高级设置**：`detail_reset_every`、`detail_tab_pool_size` 对智联平台生效，5 个 JD 参数与 BOSS 完全对齐（同一套字段、同一套默认值）。
- **智联 tab 池并行抓取**：`tab_pool_size > 1` 时后台 tab 池并行抓 JD（错峰启动、条间间隔、按 `reset_every` 导航回首页重置、平台级信号全体降级停工）；`=1` 保持原有串行路径。单条失败不中断，熔断器与结果映射语义不变。

### 修复

- **并行抓取 worker 崩溃**：默认等待函数不接受 `label` 关键字，首次错峰/间隔等待即抛 TypeError 杀死 worker（端到端实测发现）。
- **并行抓取结果错位**：缺失或重复的详情 URL 导致并行结果张冠李戴，现已单独判失败。
- **构建脚本被项目 packaging/ 目录遮蔽**：PyInstaller 在临时目录执行构建，修复 `ModuleNotFoundError: packaging.requirements`。

## [2.7.0] - 2026-08-07

### 新增

- **更新提示改版**：发现新版本时首次打开主动弹出更新窗口，提供「忽略此版本」按钮（跨启动生效）；忽略或关闭后，左上角项目名显示红点，点击项目名随时打开更新窗口。
- **顶栏手动检查更新**：新增「检查更新」按钮，绕过 24 小时缓存立即查询；更新弹窗展示上次检查时间。
- **暗色主题跨启动记忆（桌面版）**：主题偏好改由后端持久化（`~/.career-scout/theme.json`），修复桌面版随机端口导致 localStorage 失效、每次启动回到浅色的问题。

### 变更

- **智联城市目录补全**：城市码从 24 个扩至 487 个（智联官方 fe-api 全量拉取），东莞/佛山/合肥等此前缺失的城市均可正常解析；目录外城市给出友好提示。
- **环境检查精简**：移除「启动专用浏览器」一键按钮，浏览器由任务启动时自动拉起，检查项只读展示。

### 修复

- **应用内更新黑框卡死**：点击「立即重启完成更新」后替换脚本无限等待进程退出的问题。桌面壳退出时强制结束进程，替换脚本等待加 30 秒超时并自动强杀兜底（Windows/macOS）。
- **更新检查缓存路径分裂**：统一更新检查缓存路径，消除「已发布新版本但软件长期不提示」的问题。
- **智联全国抓取恒为空**：智联搜索 API 不接受 `S_SOU_WORK_CITY="0"`（全国码）会恒返回空列表；全国搜索改为不传城市字段，恢复真实全国职位。
- **智联风控误报**：收紧页面文本风险标记，移除「稍后再试」「verify」「captcha」等泛词，正常页面不再被误判为限流/封禁。

## [2.6.0] - 2026-08-07

### 新增

- **结果页平台筛选**：查看结果头部嵌入「全部 / 智联 / BOSS」小滑块，默认全部；全部视图合并展示双平台最近结果，单平台视图按来源过滤。
- **岗位来源徽章**：左侧岗位卡片与右侧详情均显示平台来源徽章（BOSS 浅绿、智联深蓝），并展示投递状态（已投/已投递）。
- **徽章跟随岗位名**：平台与投递徽章内联在岗位名称后方同一行，后续新增展示标记一律跟在岗位名之后。
- **统一指南针图标**：网页 favicon、Windows EXE 文件/窗口图标、macOS .app 图标全部换成与顶栏一致的指南针。

### 变更

- 移除结果页头部「匹配优先」文字标签与「已加载 N」计数，头部仅保留岗位数与平台滑块。
- 「开始 AI 筛选」按钮缩小一号并留出右边距；折叠卡片箭头缩小；高级执行设置数字输入框改为紧凑定宽；求职画像输入框改为多行展示并去除拖拽手柄。

### 修复

- **风控误判**：修复 BOSS 未封号时「开始筛选」误报 IP 级风控拦截的问题。连续空页在 API 正常应答（totalCount/hasMore 明确）时判定为该搜索条件无职位（非风控）；详情页限流识别改用更严格关键词，避免 JD 正文裸词误判。
- **进度条卡 9x%**：广泛抓取完成后进度条冻结在最后一帧的问题；终态与 paused 一样把显示值校正到真实锚点。

## [2.5.0] - 2026-08-07

### 新增

- **macOS 桌面版**：GitHub Actions 自动构建，支持 Apple Silicon（M 系列）Mac。
- **应用内更新**：启动自动检查新版本，顶栏提示、应用内下载（进度条 + SHA256 强制校验），一键重启完成更新。
- **顶栏 GitHub 按钮**：一键跳转项目仓库。

### 变更

- 账号弹窗改版：「打开 BOSS / 智联窗口」合并为单个「打开浏览器」；操作区右对齐。
- Windows 启动 Chrome 不再闪 CMD 黑窗。
- 移除「页面与当前后端不一致」运行时拦截；源码模式启动时自动重建前端。

## [2.4.0] - 2026-08-06

### 新增

- **Windows 桌面版（EXE）**：单个 EXE 文件下载即用，无需安装 Python/Node；pywebview 桌面窗口复用现有 Web 工作台界面，支持窗口缩放、状态记忆与单实例；抓取任务在应用内直接执行。
- **桌面版环境检查适配**：Python 依赖显示为内置运行时；新增 WebView2 运行时检查项；其余检查项与源码版一致。
- **可配置默认窗口尺寸**：`~/.career-scout/desktop_window.json` 可自定义默认窗口大小（默认 1366×768），无需重新打包。
- **多平台架构**：接入智联招聘，与 BOSS 直聘共用统一岗位模型；顶栏一键切换平台，搜索条件、筛选字段与城市选项按平台规则独立加载。
- **平台隔离的结果展示**：岗位入库时冻结来源平台身份，查看结果按平台隔离，BOSS 与智联结果互不混入。
- **岗位生命周期**：岗位详情支持标记已读、已投递、已荒废及状态纠正；已投递记录真实投递时间（可修正），跟进作为独立时间事件记录。
- **投递过期提醒**：已投递满 30 天（精确 30 × 24 小时）且无跟进的岗位进入顶部导航提醒徽标与提醒抽屉，按最长未活动时间排序，单次最多 100 条；BOSS 与智联岗位统一提醒，不按平台过滤。
- **按需 AI 行动建议**：针对单个逾期岗位请求 AI 建议，输出仅限"跟进"或"复核"；AI 不可用、失败或缺少 JD 时返回规则式兜底建议。
- **客观生命周期事件轨迹**：所有状态与时间操作追加不可覆盖事件，与"感兴趣/不感兴趣"偏好反馈分离。
- **浏览器登录态按平台隔离**：BOSS 与智联使用独立登录空间。

### 变更

- 开源协议由 MIT 调整为 Apache License 2.0。
- README 重写：覆盖多平台能力、反馈闭环功能与界面截图。
- 新增 CONTRIBUTING.md、CHANGELOG.md、SECURITY.md。

## [2.3.0] - 2026-07-30

### 已具备的能力

- BOSS 直聘 CDP 抓取（CLI + Web 工作台），支持断点续抓。
- 简历驱动的两层筛选（硬规则 + AI 语义评估），AI 不可用时降级。
- 感兴趣/垃圾桶持久区与匹配/不匹配/待确认/已筛除分类结果。
- 断点续跑、浏览器 A/B 账号、高级设置与调优实验框架、历史任务恢复。
- `job_summary` 岗位市场摘要与求职材料优化提示词。
